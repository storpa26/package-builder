# Copy to `.env` or `.env.production.local` for deployment

# WordPress base URL (production site)
VITE_WP_BASE_URL=https://cheapalarms.com.au

# Enable GoHighLevel integration in production
VITE_DISABLE_GHL_INTEGRATION=false

# GHL webhook URL (replace with your live endpoint)
VITE_GHL_WEBHOOK_URL=https://YOUR-GHL-WEBHOOK-URL